% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/encode.R
\name{encode}
\alias{encode}
\alias{fact}
\title{Encode/factor}
\usage{
encode(x, from, to, strict = FALSE, exclude = NULL)

fact(x, levels = NULL, exclude = NULL)
}
\arguments{
\item{x}{A vector of values}

\item{from, to}{Vectors of the same length. Values in \code{x} are matched to
\code{from} and replaced with the corresponding value in \code{to}.}

\item{strict}{If \code{TRUE}, values in \code{x} that are not matched to \code{from} will be
replaced with \code{NA}. If \code{FALSE}, they will be left unchanged.}

\item{exclude}{Values in \code{x} that will not be matched when recoding;
\code{exclude} will take priority over \code{from} in \code{\link[=encode]{encode()}}.}

\item{levels}{A vector of unique values. If \code{NULL}, the unique values in \code{x}
are used.}
}
\value{
\code{\link[=encode]{encode()}} A vector of the same length as \code{x} with values
replaced according to \code{from} and \code{to}.

\code{\link[=fact]{fact()}} A \code{factor} vector with levels corresponding to the
unique values in \code{x} (or \code{levels} if provided).
}
\description{
Low-level encoding and \code{factor} building
}
\details{
\code{\link[=encode]{encode()}} is a general purpose function for replacing values in a
vector.

\code{\link[=fact]{fact()}} is a low-level function for building \code{factor} vectors. It does
not perform any \code{sort()}ing of levels (unlink \code{\link[base:factor]{base::factor()}}).  Re-leveling
can be done by applying \code{\link[=encode]{encode()}} to the levels of a \code{factor} object.
}
\examples{
fact(strsplit("factor function", "")[[1L]], exclude = " ")

# encode() can be used to for the same utility as factor(x, levels, labels)
# (note: applying to levels can be more efficient)
(x <- fact(strsplit("jordan", "")[[1L]]))
levels(x) <- encode(levels(x), from = c("a", "o"), to = "*")
x
}
